%nav{aria: {label: 'breadcrumb'}}
  %ol.breadcrumb
    %li.breadcrumb-item.active{aria: {current: 'page'}} Dashboard

.row
  .col-sm-12
    

.row.mb-5
  .col-sm-4
    %h3.mt-2 
      Addresses
      - if if current_user.max_profiles > ENV['SUBSCRIPTION_MIN_PROFILE_COUNT']
        = "#{current_user.profiles}/#{current_user.max_profiles}"
    %ul.list-group
      - if @profiles.empty?
        %li.list-group-item
          Nothing here yet!
      - else
        - @profiles.order(created_at: :desc).each do |p|
          = render p
    - if current_user.max_profiles > ENV['SUBSCRIPTION_MIN_PROFILE_COUNT']
      = form_for Profile.new, url: profiles_path, html: { class: 'mt-3' } do |f|
        .input-group
          = f.text_field :name, class: 'form-control ml-1', value: '', placeholder: 'Create new address'
          = f.submit 'Create', class: 'btn btn-outline-primary ml-1'
    - else
      .alert.alert-primary
        = form_tag charges_path do
          %article
            - if flash[:error].present?
              #error_explantation
                %p=flash[:error]

            %label.amount
              %span
                Upgrade your account with up to 20 more custom addresses for £3.00/month
        
          %script.stripe-button{src: "https://checkout.stripe.com/checkout.js", data: {key: Rails.configuration.stripe[:publishable_key], description: "Upgrade to a paid subscription ", amount: "300", locale: 'auto', name: 'FairCustodian', currency: 'GBP', email: current_user.email}}

  .col-sm-8
    %h3.mt-2 All Messages

    %ul.list-group
      - if @messages.empty?
        %li.list-group-item
          Nothing here yet!
      - else
        - @messages.order(created_at: :desc).each do |m|
          = render m