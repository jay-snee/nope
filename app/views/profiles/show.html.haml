%nav{aria: {label: 'breadcrumb'}}
  %ol.breadcrumb
    %li.breadcrumb-item
      = link_to 'Dashboard', root_path
    %li.breadcrumb-item.active{aria: {current: 'page'}}
      = @profile.name

#page.mb-5{data: {id: @profile.id}}
  .row
    .col-sm-12
      .d-flex.justify-content-center.align-items-center
        .mr-auto.p-2
          .d-none.d-md-block.email
            = @profile.display_email_address
        .ml-auto
          %button.btn.btn.btn-outline-info.copy-button{data: {toggle: 'tooltip', placement: 'top', clipboard: { text: @profile.email_address}}}
            Copy Address
        - if current_user.stripe_subscription_id
          .p-2
            = link_to 'Delete profile', profile_path(@profile), method: :delete, class: 'btn btn-outline-danger', data: {confirm: 'Are you sure you want to delete this profile and all associated messages? You will not be able to reclaim this address in the future.'}
  %hr
  .row
    .col-sm-12
      %h5 Recent Messages
      %ul#messages-list.list-group.mb-5{data: {id: current_user.id}}
        - unless @profile.messages.empty?
          - @profile.messages.each do |m|
            = render m
        - else
          %li#welcome.list-group-item
            %strong Inbox is empty