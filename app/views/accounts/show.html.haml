.row
  .col-sm-8
    = pp @account.to_json
  .col-sm-4
    %ul.list-group
      %li.list-group-item
        %h4 Users
      - @account.users.each do |user|
        %li.list-group-item
          = link_to user.email, user