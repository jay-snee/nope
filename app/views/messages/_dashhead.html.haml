.d-flex
  .mr-auto
    %h2
      From:
      = @message.from
    %h3.dashhead-title
      Subject:
      = @message.subject
  .item
    = button_to message_path(@message), method: :delete, class: 'dashhead-toolbar-item btn btn-outline-danger' do
      %i.fas.fa-trash-alt
      Delete Message